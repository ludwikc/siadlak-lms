
// DEV-ONLY CONFIGURATION - REMOVE BEFORE PRODUCTION
// Toggle this to false to completely disable development login
export const ENABLE_DEV_LOGIN = true;

// Hard-coded credentials for development use only
export const DEV_CREDENTIALS: Record<string, string> = {
  'ludwikc': 'Devpass-0',
  'admin': 'admin123',
  'user': 'user123'
};
